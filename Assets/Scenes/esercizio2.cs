using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class esercizio2 : MonoBehaviour
{
    // 1. Dichiarare la variabile come pubblica:
    // public int puntiVita;

    // 2. Usare l'attributo [SerializeField] su una variabile privata:
    // [SerializeField] private int velocità;

    public int vita; 

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
